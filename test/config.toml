[bot]
plugins = [""]
plugin_dirs = ["plugins"]
adapters = ["iamai.adapter.cqhttp"]

[adapter.cqhttp]
adapter_type = "reverse-ws"
host = "127.0.0.1"
port = 8080
url = "/cqhttp/ws"
api_timeout = 100
debug = true
