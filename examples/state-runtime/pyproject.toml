[project]
name = "state-runtime"
version = "0.1.0"
description = "State iamai example."
requires-python = ">=3.11"
dependencies = [
    "iamai",
    "iamai-example-utils",
]

[tool.uv.sources]
iamai = { workspace = true }
iamai-example-utils = { workspace = true }
