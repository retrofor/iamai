[project]
name = "arcade-runtime"
version = "0.1.0"
description = "Arcade-style iamai example."
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "iamai",
]

[tool.uv.sources]
iamai = { workspace = true }
