[project]
name = "persona-rp-runtime"
version = "0.1.0"
description = "Persona roleplay iamai example."
requires-python = ">=3.11"
dependencies = [
    "iamai",
    "iamai-example-utils",
]

[tool.uv.sources]
iamai = { workspace = true }
iamai-example-utils = { workspace = true }
