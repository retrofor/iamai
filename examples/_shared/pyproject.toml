[project]
name = "iamai-example-utils"
version = "0.1.0"
description = "Shared helpers for iamai LLM examples."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.76,<4",
    "pydantic>=2.11,<3",
]
