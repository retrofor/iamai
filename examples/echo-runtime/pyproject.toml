[project]
name = "echo-runtime"
version = "0.1.0"
description = "Example iamai application."
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "iamai",
]

[tool.uv.sources]
iamai = { workspace = true }
