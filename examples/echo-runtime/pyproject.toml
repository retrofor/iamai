[project]
name = "echo-runtime"
version = "0.1.0"
description = "Example iamai application."
requires-python = ">=3.10"
dependencies = [
    "iamai",
]

[tool.uv.sources]
iamai = { workspace = true }
