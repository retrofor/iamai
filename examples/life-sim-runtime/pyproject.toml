[project]
name = "life-sim-runtime"
version = "0.1.0"
description = "Life simulator iamai example."
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "iamai",
    "iamai-example-utils",
]

[tool.uv.sources]
iamai = { workspace = true }
iamai-example-utils = { workspace = true }
